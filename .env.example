# Optional GitHub token (recommended to increase rate-limit budget).
OPENINGS_GITHUB_TOKEN=

# Build controls
MAX_ISSUES_PER_REPOSITORY=30
MAX_REPOSITORIES=0
REQUEST_DELAY_MS=120

# Optional segmented build filter (comma-separated country codes, e.g. BR,US,PT)
COUNTRY_CODES=
